<div>
    <table border="1">
        <tr>
            <th>id</th>
            <th>first name</th>
            <th>last name</th>
            <th>mail</th>
            <th>login</th>
            <th>password</th>
            <th>role</th>
            <th>edit</th>
            <th>delete</th>
        </tr>
        <?php
            //$this->usersArray est une variable qui peut être garnie par le controller
            //elle doit recevoir un tableau contenant des objets de type Default_Model_Users
            //le tableau est parcouru par foreach pour afficher les propiétés des objets contenus dans le tableau
            foreach ($this->usersArray as $users) :
        ?>
        <tr>
            <td><?php echo $users->id; ?></td>
            <td><?php echo $users->firstname; ?></td>
            <td><?php echo $users->lastname; ?></td>
            <td><?php echo $users->mail; ?></td>
            <td><?php echo $users->login; ?></td>
            <td><?php echo $users->password; ?></td>
            <td><?php echo $users->role; ?></td>
            <td>
                <a href="<?php echo $this->url(array('controller' => 'users', 'action' => 'edit', 'id' => $users->id), 'default', true); ?>">Edit</a>
            </td>
           <td>
                <a href="<?php echo $this->url(array('controller' => 'users', 'action' => 'delete', 'id' => $users->id), 'default', true); ?>">Delete</a>
            </td>
        </tr>
        <?php endforeach; ?>
    </table>
    <?php
    //nous affichons, à la suite du tableau (après la balise </table>), notre template qui permettra de gérer la pagination
    //la méthode pginationControl() prend, ici, 3 arguments:
    //  - l'instane paginator
    //  - le style de défilement
    //  - le template de pagination
    echo $this->paginationControl($this->usersArray, 'Sliding', 'common/my_paginator_control.phtml');
	?>
</div>